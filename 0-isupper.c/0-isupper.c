#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character
 * @c: VAriable text
 * Return: Always 0.
 */

int _isupport(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
